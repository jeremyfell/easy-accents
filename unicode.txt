symbol = "é"

unicode = symbol.charCodeAt().toString(16)

while (unicode.length < 4) {
	unicode = "0" + unicode;
}


// Symbol change
newInput.addEventListener("change", function() {
	symbol = this.value;
	
	unicode = symbol.charCodeAt().toString(16)

	while (unicode.length < 4) {
		unicode = "0" + unicode;
	}
	
	unicode_input.value = unicode;
	
});


// Unicode change
newInput2.addEventListener("change", function() {
	unicode = this.value
	
	if (unicode.length !== 4) invalid;
	
	if any non numeric characters, invalid;
	
	symbol = String.fromCharCode(parseInt(unicode));
	
	symbol_input.value = symbol;
});